# 儿童声音讲故事短视频项目 — 技术方案设计（零成本版）

> 版本：v2.1 ｜ 日期：2026-02 ｜ 状态：方案确认稿
> 定位：从「主题/框架 → 写故事 → 改编 → 配音 → 动画」到「儿童声音旁白 + 水彩/卡通风格动画」的 9:16 竖屏短视频自动化流水线，目标发布平台：抖音 / 快手。
> **核心成本约束（用户明确要求）**：声音克隆允许一次性投入；**视频出片全流程每次零花费**（不采用任何按量计费的云服务）。

---

## 1. 项目概述

### 1.1 一句话描述

输入**主题或框架**，大模型先写故事（可反复整篇重写、局部修改细节），再自动产出「儿童声音讲述 + 风格化动画画面 + 大字幕 + 配乐」的竖屏短视频，**出片边际成本 ≈ 0（仅 LLM 花费 ¥0.05~0.15）**。

### 1.2 核心链路

```
主题 / 框架（你给方向，可选）
   │
   ▼
① 故事创作           （LLM 写故事：支持整篇重写、局部修改细节，直到你满意）
   │                    （国内大模型 API，可花费，含迭代）
   ▼
② 文案改编与分镜     （LLM：定稿故事 → 口语化旁白稿 + 逐镜画面描述 + 时间轴规划）
   │                    （国内大模型 API，可花费 ~¥0.02/条）
   ▼
③ 声音克隆与配音     （本地开源模型：一次性克隆儿童音色，之后永久免费合成）
   │
   ▼
④ AI 文生图          （一次性批量出图 + 本地图库缓存，逐镜 9:16 竖屏画面）
   │
   ▼
⑤ 程序化动画         （Python/FFmpeg：镜头推拉摇移 + 微动效 + 时长对齐）
   │
   ▼
⑥ 字幕               （大字幕 + 关键词高亮，抖音风）
   │
   ▼
⑦ 合成导出           （FFmpeg：拼接 + 配音 + BGM 自动避让 + 封面 → 1080×1920 MP4）
```

### 1.3 已确认的关键决策

| 决策项 | 选择 | 说明 |
|---|---|---|
| 交付目标 | 先出方案文档 | 本文档，确认后再进入编码 |
| 成本约束 | **视频每次零花费** | TTS 合成、文生图、动画合成全部零花费；**LLM（写故事 + 文案分镜）可花费**（国内大模型 API） |
| 故事创作 | LLM 写故事（可花费） | 输入主题/框架 → 生成故事 → 支持**整篇重写**与**局部细节修改** → 定稿后进入分镜 |
| 声音克隆 | **本地开源模型**（一次性克隆） | CosyVoice2-0.5B 零样本克隆（主）/ GPT-SoVITS（备），克隆一次永久免费合成 |
| 视频画面 | 程序化动画 | AI 静态图 + 镜头运动/微动效，本地计算零成本；未来可无缝升级图生视频 |
| 画幅平台 | 9:16 竖屏 | 1080×1920，30~60 秒，适配抖音/快手 |

**为什么不用云 API 克隆（MiniMax / 火山引擎）**：克隆本身是一次性付费，但**之后每次合成旁白都按字符持续计费**（每条 ¥0.2~0.5，量大了更贵）——不符合"视频零花费"约束。本地模型把"克隆"和"合成"都搬到自己的电脑上，出片成本恒为 0（仅电费）。

### 1.4 本机硬件实测（2026-02）

| 项目 | 实测结果 | 对方案的影响 |
|---|---|---|
| CPU | Intel i5-1135G7（4 核 8 线程，2.4GHz） | CPU 推理可行但偏慢；TTS/动画合成没问题 |
| 显卡 | Intel Iris Xe 核显（**无 NVIDIA 独显，无 CUDA**） | 本地跑 SDXL/FLUX 不现实 → M3 文生图改走"**一次性批量出图**"模式（§3.4） |
| 结论 | 无独显笔记本 | M1 国内大模型 API（省内存）、M2 本地 CPU 推理、M3 一次性租云 GPU 或免费额度批量出图、M4-M6 本地计算 |

---

## 2. 总体架构

### 2.1 模块划分

| 模块 | 职责 | 技术选型（零成本方案） |
|---|---|---|
| M0 故事创作 | 主题/框架 → 故事草稿（可重写/局部修改）→ 定稿 | 国内大模型 API（可花费），交互式 CLI |
| M1 文案改编 | 故事 → 口语化旁白 + 分镜表（JSON） | 国内大模型 API（DeepSeek / 通义 / 豆包 / GLM 等，配置化切换）；Ollama 离线备选 |
| M2 配音 | 声音克隆（一次性）+ 逐镜合成 + 时间轴 | **CosyVoice2-0.5B（本地）**，备选 GPT-SoVITS |
| M3 文生图 | 分镜描述 → 风格统一竖屏图 | 本地 SDXL / FLUX.1-schnell（diffusers 或 ComfyUI） |
| M4 动画 | 静态图 → 动态镜头（Ken Burns + 微动效） | Python + OpenCV + FFmpeg |
| M5 字幕 | 旁白 → 带时间轴的大字幕（含高亮） | 文本驱动生成 ASS，无需 ASR |
| M6 合成 | 拼镜头、混音、BGM ducking、封面、导出 | FFmpeg |
| M7 编排 | 一键端到端 CLI + 配置 | Python 3.11，`make_video.py` |

### 2.2 数据流与中间产物

```
主题/框架（你输入）
   → M0 → stories/drafts/xxx_v1.md …（故事草稿：重写/修改产生新版本，可回退）
   → 定稿 stories/xxx.md（你确认的故事）
   → M1 → storyboard.json        （分镜表，全流程的"剧本"）
   → M2 → audio/scene_01.wav …  （逐镜旁白 + 每镜真实时长）
   → M3 → images/scene_01.png …  （9:16 风格化画面）
   → M4 → clips/scene_01.mp4 …   （动态镜头，时长=旁白+padding）
   → M5 → subs/final.ass         （字幕轨）
   → M6 → output/xxx_1080x1920.mp4
```

**关键设计**：`storyboard.json` 是唯一事实来源，M2/M3/M4 都只依赖它。这意味着：
- 换 TTS 模型、换文生图模型、换动画方案，都不需要动其他模块；
- 未来升级「AI 图生视频」（可灵/即梦/Runway）时，只需替换 M4，输入输出接口不变（届时才需按次付费，属可选升级）。

### 2.3 分镜表 Schema（示例）

```json
{
  "title": "小狐狸的月亮灯",
  "style": "watercolor",
  "story_id": "fox_moon",
  "scenes": [
    {
      "id": 1,
      "narration": "从前，有一只小狐狸，它最喜欢在夜里看月亮。",
      "image_prompt": "水彩画风格，橙色小狐狸坐在山坡上仰望夜空，圆月，星星，柔和渐变的蓝紫色天空，温馨童话感，竖构图",
      "camera": { "move": "zoom_in", "start_scale": 1.0, "end_scale": 1.15 },
      "effects": ["twinkle_stars", "fireflies"],
      "keywords": ["月亮"]
    }
  ]
}
```

- `narration`：该镜旁白台词（口语化，≤40 字/镜，读起来 ~5 秒）；
- `camera.move`：`zoom_in / zoom_out / pan_left / pan_right / pan_up / pan_down / none`；
- `effects`：微动效标签（见 §3.5），空数组表示纯镜头运动；
- `keywords`：字幕高亮词（每镜 1~2 个，强化"儿童故事"记忆点）。

---

## 3. 模块设计

### 3.1 M0 故事创作（LLM，可花费）

**定位**：全流程第一步。你只给**主题**（或再加**框架**：角色、地点、情节走向、结局倾向），大模型写完整故事；不满意可**整篇重写**或**改局部细节**，直到你认可再进分镜。

- **输入（CLI 参数 / `config.yaml`）**：
  - `theme`：主题（必填，如"小狐狸和月亮""森林里的小雨伞"）；
  - `framework`（可选）：你提供的框架/要素（角色、冲突、结局……）；
  - `style_hint`：画面风格倾向（水彩/卡通，影响故事基调描写）；
  - `target_chars`：目标字数（默认 200~400 字，对应 30~60 秒视频）。
- **处理（可花费，国内大模型 API，模型清单见 §3.2）**：
  1. **生成**：按"儿童睡前故事"人设写完整故事（标题 + 正文），落盘 `stories/drafts/xxx_v1.md`；
  2. **审阅循环（交互式 CLI）**：打印故事后你三选一——
     - `重写 <反馈>`：带反馈（如"太长了 / 不够有趣 / 结局太悲伤"）**整篇重写** → 生成 v2；
     - `修改 <指令>`：**局部修改**（如"把狐狸改成小兔子""结局改成大家一起回家"）→ 只改指定细节、其余逐字保留 → 生成新版本；
     - `接受`：定稿为 `stories/xxx.md`，进入 M1 分镜。
  3. **版本管理**：每次重写/修改产生新版本文件（v1/v2/…），旧版保留可回退对比。
- **实现要点**：
  - "局部修改"的 prompt 明确要求"**仅改动指定细节，其余内容逐字保留，不擅自扩写**"；
  - 更精细的编辑可**直接打开 md 文件手工改**——最终以文件内容为准（最灵活，适合逐字打磨）；
  - 定稿校验：非空、字数在目标区间、无占位符/未完句。

### 3.2 M1 文案改编与分镜（LLM，可花费）

- **输入**：M0 定稿故事 `stories/xxx.md`（任意长度，建议 ≤2000 字）。
- **处理**：一个带结构化输出（JSON）的 LLM 调用，一次完成：
  1. 压缩/改写为 8~14 个镜头，每镜 ≤40 字口语化旁白（符合儿童故事节奏）；
  2. 为每镜写画面描述 `image_prompt`（含统一风格前缀，见 §4.2）；
  3. 规划镜头运动与微动效；
  4. 抽取每镜 1~2 个高亮关键词。
- **输出校验**：JSON Schema 校验 + 兜底重试（失败自动重试一次并换 prompt 变体）。
- **选型（可花费，国内大模型 API 主选）**：本环节允许花费（约 ¥0.01~0.05/条），优先用**国内大模型 API**保证分镜质量与速度；本地 Ollama 仅作离线备选。**统一走 OpenAI 兼容格式，`config.yaml` 里一个 `provider` 字段即可切换**：

| 厂商 | 模型示例 | OpenAI 兼容 Base URL（以官方文档为准） |
|---|---|---|
| DeepSeek | deepseek-chat（V3） | `https://api.deepseek.com` |
| 阿里百炼·通义 | qwen-plus / qwen-max | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| 火山方舟·豆包 | doubao-pro 系列 | `https://ark.cn-beijing.volces.com/api/v3` |
| 智谱 | glm-4-flash（免费档）/ glm-4-plus | `https://open.bigmodel.cn/api/paas/v4` |
| Moonshot Kimi | kimi-k2 系列 | `https://api.moonshot.cn/v1` |
| MiniMax | MiniMax-Text-01 | `https://api.minimax.chat/v1` |

  - 推荐起步：**DeepSeek**（便宜、分镜质量好）或 **智谱 GLM-4-Flash**（长期免费档，花费为 0）。
  - **本地 Ollama**（离线备选）：`qwen2.5:3b/7b`，本机 CPU 一次分镜 3~8 分钟，仅断网或不想付费时用。

### 3.3 M2 声音克隆与配音（本地开源模型，一次性克隆）

**流程**：
1. **克隆阶段（一次性，免费）**：录一段儿童声音样本（10 秒即可，1~3 分钟更稳，采集规范见 §4.6）→ 用本地模型克隆出专属音色。
2. **合成阶段（每条视频，免费）**：按分镜表逐镜调用本地模型推理，传 `参考音频 + text + 语速/情感参数`，得到每镜音频。**无限次合成，永远不花钱**。
3. **时间轴**：逐镜合成后即知每镜真实时长；本地模型一般无字级时间戳，字幕按"整句时长均匀分配给各字"近似（§3.6）。

**本地模型对比**：

| 模型 | 克隆方式 | 所需样本 | 显存要求 | 特点 | 参考 |
|---|---|---|---|---|---|
| **CosyVoice2-0.5B（主选）** | **零样本克隆**（无需训练） | 3~10 秒即可 | 可优化至 <3G，低配 GPU 可跑 | 阿里开源，中文母语效果好；克隆即插即用，换音色零成本 | [部署指南](https://blog.csdn.net/weixin_35757531/article/details/158213037)、[显存优化 <3G](https://blog.csdn.net/weixin_34374684/article/details/158136367) |
| GPT-SoVITS（备选） | 需微调训练（一次性） | 1~5 分钟 | 训练 ~6-8G | 效果更稳、可控性强（语气/停连），适合当主模型效果不满意时切换 | [部署与实测](https://blog.csdn.net/weixin_35995661/article/details/156215067) |

> **本机（i5-1135G7，无独显）可行**：CosyVoice2 纯 CPU 推理，一条 5 秒旁白约 10~30 秒，一条 30~60 秒视频的配音约 3~6 分钟，每周 1~3 条完全可接受。克隆与合成都在本地，永久免费。

**儿童声线建议**：克隆目标音色建议 6~12 岁儿童；合成参数建议 `语速 0.95~1.05`、情感偏"讲述感/好奇感"，避免过度夸张。

### 3.4 M3 AI 文生图（一次性批量出图，零成本）

- **分辨率**：1080×1920（9:16）直接生成，避免裁切破坏构图；模型若只支持方图，则生成方形后对上下做 outpaint 外扩。
- **选型**：
  - **本机无独显 → "一次性批量出图"模式（推荐）**：M3 不依赖本机算力，改成周期性批量任务：
    1. **租云 GPU 批量生成（推荐，¥5~15/批）**：AutoDL / 恒源云 / 阿里云 GPU 实例（约 ¥1~2/小时），一次租 4~8 小时批量生成 50 条视频的全部画面（约 500 张，SDXL 单张 5~10 秒），下载到本地图库缓存，之后出片直接用缓存图——**出片时零花费**；
    2. **免费额度 API（¥0/批）**：硅基流动 SiliconFlow、火山方舟等新人注册送额度，一次性生成几十~几百张，用完即止——属"一次性投入"；
    3. **本机 CPU 兜底（仅应急）**：1080×1920 一张要几十分钟，建议降为 768×1344。
  - **未来若加独显**：SDXL（≥6G）/ FLUX.1-schnell（≥8G）本地直出，单张 5~20 秒，彻底免租。代码层面 M3 做成可插拔（本地/API/云 GPU 三选一，见 §6）。
- **风格统一策略（关键）**：
  1. 固定风格前缀：水彩 = `"watercolor illustration, soft pastel palette, fairy tale picture book style, gentle brush strokes, dreamy light, 竖构图"`；卡通 = `"flat cartoon style, vibrant colors, cute rounded characters, children's picture book, clean outlines, 竖构图"`；
  2. 固定负面提示词（如 `blurry, deformed, extra fingers, text, watermark`）；
  3. 固定 LoRA 与 seed 分段，保证同一故事内画面一致；
  4. 可选后处理：统一色温/对比度（FFmpeg 或 PIL）。

### 3.5 M4 程序化动画（核心差异化模块）

静态图转动态镜头，全部本地计算，零 API 成本。

- **镜头运动（Ken Burns）**：用 OpenCV/numpy 对图做仿射变换序列（zoom/pan），输出 30fps 帧序列：
  - `zoom_in`：scale 1.0 → 1.15（中心或锚点）；
  - `zoom_out`：1.15 → 1.0；
  - `pan_left/right/up/down`：视口沿方向平移；
  - 运动曲线用 ease-in-out，避免机械感。
- **微动效（图层合成）**：在静态图上叠加动态元素：
  - `twinkle_stars`（星光闪烁）、`fireflies`（萤火虫漂移）、`clouds_drift`（云朵横移）、`rain` / `snow` / `falling_leaves`、`water_ripple`（水面涟漪）、`light_rays`（光束扫过）、`bubbles`（气泡上浮）。
  - 实现：预生成透明 PNG 粒子精灵，用 numpy 做逐帧位移 + 透明度正弦变化，叠加回主图。
- **时长对齐**：镜长 = `max(该镜旁白时长 + 0.5s padding, 最小 3s)`，保证画面不赶、不空。
- **输出**：每镜一段 MP4（H.264，30fps，1080×1920）。
- **未来扩展**：同一分镜表直接对接可灵/即梦/Runway 图生视频接口（按次付费，可选升级），替换本模块即可。

### 3.6 M5 字幕

- 文本已知（分镜表 `narration`），**无需 ASR**：按每镜时间轴生成 ASS 字幕。
- 抖音风样式：白色大号字（约屏宽 8%）+ 黑色粗描边 + 底部安全区；`keywords` 高亮词换色/加粗。
- 本地模型无字级时间戳 → 用**近似时间轴**：整句时长按字数均匀分配（儿童故事句子短，观感足够）；高亮词在句中做静态变色而非逐字动画（省事且稳定）。

### 3.7 M6 合成导出（FFmpeg）

- 拼接：所有镜头 clip 按时间轴 concat；
- 音轨：旁白轨 + BGM 轨（BGM 用 `sidechaincompress` 做 ducking：旁白响起时 BGM 自动 -8~-12dB）；
- 烧字幕：`ass` 滤镜硬烧；
- 封面：首帧 + 独立生成的标题图（本地文生图一次）；
- 输出规格：H.264 / 30fps / 1080×1920 / 码率 8~10 Mbps / AAC 192kbps / 时长 30~60 秒（抖音主推 30s+ 完播友好）。

---

## 4. 关键工程要点

### 4.1 时间轴一致性

所有模块以「分镜表 + 每镜音频真实时长」为时间基准：M2 先产出音频 → M4 动画时长跟随音频 → M5 字幕跟随分镜时间轴。任何模块改动不会造成音画错位。

### 4.2 Prompt 工程

- 风格前缀常量放在 `config.yaml`，一次修改全局生效；
- `image_prompt = 风格前缀 + 分镜画面描述 + 竖构图后缀 + 负面提示`；
- 同一故事固定 seed 池，保证人物形象跨镜一致（进阶：给主角固定描述短语，如"橙色小狐狸，戴蓝色围巾"）。

### 4.3 字体与版权

- 字幕字体：阿里巴巴普惠体 / 思源黑体 / 站酷快乐体（确认免费商用条款）；
- BGM：使用免版权曲库（如爱给网、Free Music Archive、抖音开放平台授权音乐）；
- 故事版权：优先原创或公版（安徒生/格林等），商用需规避未授权 IP。

### 4.4 合规与伦理（必读）

- **声音克隆授权**：必须使用本人或获得明确授权的儿童声音样本（监护人同意）。本项目仅用于**自家账号内容**，禁止用于冒充他人。本地模型无平台审核，但**伦理责任在你自己**。
- **AI 内容标注**：抖音/快手对 AI 生成内容有标注要求（发布时勾选"AI 生成"），平台规则变化需持续关注。
- **未成年人内容**：内容面向儿童，避免恐怖/暴力画面，文案积极正面。

### 4.5 硬件检测（先确认能不能本地跑）

查显卡（Windows）两种方法任选：

1. **任务管理器**：按 `Ctrl+Shift+Esc` → 「性能」→ 左侧选「GPU」→ 右上角看名称和「专用 GPU 内存」（如 8.0 GB）。
2. **命令行**：`Win+R` 输入 `dxdiag` 回车 → 「显示」选项卡看"名称 / 显示内存"。

判读：
- 有 NVIDIA 显卡且显存 ≥6G → 全流程本地跑，**推荐路线**；
- 有 NVIDIA 显卡但 <6G → 可跑 CosyVoice2（<3G）+ 小显存文生图方案，或调低分辨率；
- 无独显（只有核显）→ 走 CPU 慢速路线 + 一次性租 GPU（§5.2）。

> **本机实测结果**：Intel i5-1135G7（4 核）+ Intel Iris Xe 核显，**无 NVIDIA 独显（无 CUDA）**。结论：M1 用国内大模型 API（可花费）、M2 用 CPU 推理、M3 用一次性批量出图（§3.4）、M4-M6 本地计算。内存建议 ≥16G；若只有 8G，M1 走 API 可省内存给 M2。

### 4.6 声音样本采集规范（一次性，之后永久使用）

- **设备**：手机即可（录音 App），或电脑麦克风；保证 10 秒~3 分钟干净人声；
- **环境**：安静房间、关门关窗，无 BGM、无回声、无翻页/键盘声；
- **距离**：手机距嘴 10~20cm，固定放置避免摩擦声；
- **语气**：自然讲故事的语气，念一段童谣/小短文（不要播音腔、不要逐字念）；
- **格式**：导出 WAV 或高码率 MP3（≥320kbps），单人声；一段即可，无需剪辑；
- **存档**：`assets/voice_sample/` 下保存原始文件 + 备份。一次录制，克隆后全部视频共用。

---

## 5. 成本与性能估算

### 5.1 单条视频成本（30~60 秒，约 10 镜）—— 零花费

| 环节 | 方案 | 每条花费 |
|---|---|---|
| 故事创作（M0） | 国内大模型 API（可花费，含重写/修改迭代） | **~¥0.02~0.1** |
| 文案改编（M1） | 国内大模型 API（可花费） | **~¥0.02** |
| 声音合成（M2） | 本地模型推理（克隆已一次性完成） | **¥0** |
| 文生图 10 张（M3） | 一次性批量出图（缓存图库，§3.4） | **¥0** |
| 动画/字幕/合成（M4-M6） | FFmpeg + OpenCV 本地计算 | **¥0** |
| BGM | 免版权曲库 | **¥0** |
| **单条合计** | | **≈ ¥0.05~0.15（LLM 写故事+分镜，含迭代）+ 电费，可忽略** |

### 5.2 一次性投入（可选，按你的硬件决定）

| 场景 | 一次性投入 | 说明 |
|---|---|---|
| **本机方案（已确认无独显）** | **¥0~15** | M1 国内大模型 API（~¥0.02/条）、M2 本地 CPU 推理、M3 一次性租云 GPU 批量出图（¥5~15/批，可用几周）或免费额度 API（¥0） |
| 未来加独显（可选） | ¥300~600（二手 8G N 卡，仅台式机） | 全流程本地免费、速度最快；笔记本无法直接加卡，非必需 |

### 5.3 效率

- 首次搭建（含克隆、装环境）：1~2 天；
- 跑通后单条全自动：**10~20 分钟/条**（GPU）；纯 CPU 会慢 3~5 倍，可过夜批量；
- 批量：`make_video.py --batch stories/*.txt` 可整晚批量出片。

---

## 6. 目录结构与技术栈（建议）

```
story-video/
├── README.md
├── config.yaml            # 模型路径、风格前缀、画幅、字体、BGM 路径
├── stories/               # 主题输入、故事草稿（drafts/，版本化）、定稿故事（.md）
├── pipeline/
│   ├── story_gen.py       # M0 故事创作（生成/重写/局部修改，交互式）
│   ├── script_llm.py      # M1 文案→分镜（国内大模型 API，配置化）
│   ├── tts_clone.py       # M2 CosyVoice2 克隆（一次性）+ 逐镜合成
│   ├── image_gen.py       # M3 本地 SDXL/FLUX 文生图
│   ├── animate.py         # M4 镜头运动 + 微动效
│   ├── subtitles.py       # M5 ASS 字幕
│   ├── compose.py         # M6 FFmpeg 合成
│   └── make_video.py      # M7 编排入口
├── assets/                # 声音样本、字体、粒子素材、BGM
├── output/                # 中间产物 + 成片
└── requirements.txt       # openai, requests, opencv-python, numpy, Pillow, pyyaml
```

**技术栈**：Python 3.11 ｜ LLM：国内大模型 API（DeepSeek/通义/豆包/GLM/Kimi/MiniMax，OpenAI 兼容，配置化切换），Ollama 离线备选 ｜ TTS：**CosyVoice2-0.5B（本地，主）**，GPT-SoVITS（备）｜ 图像：一次性批量出图（云 GPU 或免费额度）＋本地图库缓存，未来可加独显本地直出 ｜ 动画：OpenCV + numpy ｜ 合成：FFmpeg。

---

## 7. 实施计划

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| Phase 0 | ① 查显卡（§4.5）→ ② 装 Python 3.11 + FFmpeg → ③ 按 §4.6 录 10 秒~3 分钟儿童声音样本 → ④ 下载 CosyVoice2 模型并克隆音色 | 克隆音色能合成出一句自然的中文样音 |
| Phase 1 | M0 故事创作 + M1 分镜：交互式生成/重写/局部修改 + prompt 工程 + JSON 校验 | 主题→故事→分镜表全流程可用，支持重写与局部修改 |
| Phase 2 | M2 配音：逐镜合成 + 时间轴落盘 | 旁白音频+每镜时长正确 |
| Phase 3 | M3 文生图：本地出图、风格统一、9:16 | 一镜多图风格肉眼一致 |
| Phase 4 | M4+M5+M6：动画、字幕、合成 | 出一条 30 秒完整样片 |
| Phase 5 | 端到端 `make_video.py` + 批量 + 参数微调 | `python make_video.py stories/示例.txt` 一键出片 |

---

## 8. 风险与备选

| 风险 | 影响 | 应对 |
|---|---|---|
| 本机无独显，出图慢（已确认） | 出片慢 | M3 走一次性批量模式（租云 GPU ¥5~15 或免费额度）；M1 用国内大模型 API；M2/M4-M6 本地 CPU 完全可接受 |
| CosyVoice2 克隆不像/不稳定 | 出片质量 | 提高样本质量（§4.6）；换 GPT-SoVITS 微调（备选模型）；调语速/情感参数 |
| 分镜质量一般 | 文案生硬 | 换更强大模型（DeepSeek V3 / qwen-max / GLM-4-plus）或优化 prompt |
| 故事同质化/不生动 | 内容质量 | 提供更具体的主题与框架；多版本对比择优；换更强模型 |
| 文生图人物跨镜不一致 | 观感割裂 | 固定主角描述短语 + seed 池 + 必要时参考图（IP-Adapter） |
| 平台规则变化（AI 标注、儿童内容） | 限流/下架 | 关注抖音/快手创作者规则，内容合规优先 |
| 程序化动画显得"静态" | 完播率 | 增加微动效种类、镜头节奏变化；后续可选升级图生视频（那时才需按次付费） |

---

## 9. 下一步（确认后执行）

1. 确认本文档（成本规则更新：**仅 LLM 可花费**；§5 一次性投入项）；
2. 你去准备：**① 按 §4.6 录 10 秒~3 分钟儿童声音样本**（手机即可）；② **任一国内大模型 API key**（DeepSeek / 通义 / 豆包 / GLM / Kimi 等，OpenAI 兼容，清单见 §3.2）；
3. 我按 Phase 0→5 逐模块编码（先搭工程骨架 + 写 **M0 故事创作**（含重写/局部修改）+ M1 分镜），每阶段给出可运行验证点。
